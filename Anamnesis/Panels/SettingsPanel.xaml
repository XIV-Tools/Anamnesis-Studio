<local:PanelBase
	x:Class="Anamnesis.Panels.SettingsPanel"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:XivToolsWPF="clr-namespace:XivToolsWpf.Controls;assembly=XivToolsWpf"
	xmlns:ana="clr-namespace:Anamnesis"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:Anamnesis.Panels"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
	Title="[Navigation_Settings"
	Width="800"
	Height="512"
	Icon="Cogs"
	mc:Ignorable="d">

	<Grid x:Name="ContentArea">

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<GroupBox
			Grid.Column="0"
			Style="{StaticResource PanelGroupBox}">

			<GroupBox.Header>
				<XivToolsWPF:Header
					Icon="IdCard"
					Text="[Settings_InterfaceHeader" />
			</GroupBox.Header>

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<XivToolsWPF:IconBlock
					Grid.Column="0"
					HorizontalAlignment="Right"
					FontSize="22"
					Icon="Language" />
				<TextBlock
					Grid.Row="0"
					Grid.Column="0"
					Margin="10,0,35,0"
					Style="{StaticResource Label}"
					Text="[Settings_Language]" />
				<ComboBox
					Grid.Row="0"
					Grid.Column="1"
					Margin="6"
					ItemsSource="{Binding Languages, Mode=OneTime}"
					SelectedValue="{Binding SelectedLanguage}">
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Display}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

				<TextBlock
					Grid.Row="1"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_AlwaysOnTop" />
				<CheckBox
					Grid.Row="1"
					Grid.Column="1"
					Margin="6,3"
					IsChecked="{Binding SettingsService.Settings.AlwaysOnTop}" />

				<TextBlock
					Grid.Row="2"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_Overlay" />
				<CheckBox
					Grid.Row="2"
					Grid.Column="1"
					Margin="6,3"
					IsChecked="{Binding SettingsService.Settings.OverlayWindow}" />

				<TextBlock
					Grid.Row="3"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_WindowSize" />
				<ComboBox
					x:Name="SizeSelector"
					Grid.Row="3"
					Grid.Column="1"
					Margin="6,3"
					SelectedValue="{Binding SettingsService.Settings.Scale}" />

				<TextBlock
					Grid.Row="4"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_Translucency" />
				<CheckBox
					Grid.Row="4"
					Grid.Column="1"
					Margin="6,3"
					IsChecked="{Binding SettingsService.Settings.EnableTranslucency}" />

				<TextBlock
					Grid.Row="5"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_WindowOpacity" />
				<XivToolsWPF:NumberBox
					x:Name="TransparancySlider"
					Grid.Row="5"
					Grid.Column="1"
					Margin="6,3"
					IsEnabled="{Binding SettingsService.Settings.EnableTranslucency}"
					Maximum="1"
					Minimum="0.1"
					Slider="Absolute"
					TickFrequency="0.1"
					Value="{Binding SettingsService.Settings.Opacity}" />

				<TextBlock
					Grid.Row="6"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_Font" />
				<!--<CheckBox IsChecked="{Binding SettingsService.Settings.UseHyperlegibleFont}" Grid.Column="1" Grid.Row="6" Margin="6, 3"/>-->

				<ComboBox
					Grid.Row="6"
					Grid.Column="1"
					Width="150"
					HorizontalAlignment="Left"
					ItemsSource="{Binding Fonts}"
					SelectedItem="{Binding SelectedFont}">

					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Key}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

				<TextBlock
					Grid.Row="7"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_WrapRotations" />
				<CheckBox
					Grid.Row="7"
					Grid.Column="1"
					Margin="6,3"
					IsChecked="{Binding SettingsService.Settings.WrapRotationSliders}" />

				<TextBlock
					Grid.Row="8"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_Developer" />
				<CheckBox
					Grid.Row="8"
					Grid.Column="1"
					Margin="6,3"
					IsChecked="{Binding SettingsService.Settings.EnableDeveloperTab}" />

				<GroupBox
					Grid.Row="9"
					Grid.ColumnSpan="2">
					<GroupBox.Header>
						<TextBlock Text="[Settings_Theme" />
					</GroupBox.Header>

					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
						</Grid.ColumnDefinitions>

						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>

						<TextBlock
							Grid.Row="0"
							Grid.Column="0"
							Style="{StaticResource Label}"
							Text="[Settings_Theme_Override" />
						<CheckBox
							Grid.Row="0"
							Grid.Column="1"
							Margin="6"
							IsChecked="{Binding SettingsService.Settings.OverrideSystemTheme}" />

						<TextBlock
							Grid.Row="1"
							Grid.Column="0"
							IsEnabled="{Binding SettingsService.Current.OverrideSystemTheme}"
							Style="{StaticResource Label}"
							Text="[Settings_Theme_Light" />
						<CheckBox
							Grid.Row="1"
							Grid.Column="1"
							Margin="6"
							IsChecked="{Binding SettingsService.Settings.ThemeLight}"
							IsEnabled="{Binding SettingsService.Current.OverrideSystemTheme}" />

						<TextBlock
							Grid.Row="2"
							Grid.Column="0"
							IsEnabled="{Binding SettingsService.Current.OverrideSystemTheme}"
							Style="{StaticResource Label}"
							Text="[Settings_Theme_Color" />
						<wpf:ColorPicker
							Grid.Row="2"
							Grid.Column="1"
							IsEnabled="{Binding SettingsService.Current.OverrideSystemTheme}"
							Color="{Binding SettingsService.Settings.ThemeTrimColor}" />

					</Grid>
				</GroupBox>

				<!--  Gallery options  -->
				<GroupBox
					Grid.Row="10"
					Grid.ColumnSpan="2">
					<GroupBox.Header>
						<TextBlock Text="[Settings_GalleryHeader" />
					</GroupBox.Header>

					<Grid
						Grid.Row="7"
						Grid.ColumnSpan="2"
						Margin="0,0,4,0">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>

						<Grid.RowDefinitions>
							<RowDefinition />
							<RowDefinition />
						</Grid.RowDefinitions>

						<TextBlock
							Grid.Row="0"
							Grid.Column="0"
							Style="{StaticResource Label}"
							Text="[Settings_Gallery" />
						<ComboBox
							x:Name="GalleryCombobox"
							Grid.Row="0"
							Grid.Column="1"
							Grid.ColumnSpan="2"
							Width="200"
							Margin="1"
							HorizontalAlignment="Left"
							SelectionChanged="OnGalleryChanged">
							<ComboBoxItem>
								<TextBlock Text="[Settings_Gallery_None" />
							</ComboBoxItem>
							<ComboBoxItem>
								<TextBlock Text="[Settings_Gallery_Curated" />
							</ComboBoxItem>
							<ComboBoxItem>
								<TextBlock Text="[Settings_Gallery_Local" />
							</ComboBoxItem>
						</ComboBox>

						<TextBox
							Grid.Row="1"
							Grid.Column="1"
							Margin="3,0,0,0"
							IsEnabled="False"
							Style="{StaticResource MaterialDesignTextBox}"
							Text="{Binding SettingsService.Settings.GalleryDirectory}" />
						<Button
							Grid.Row="1"
							Grid.Column="2"
							Margin="6,3,0,0"
							Click="OnBrowseGallery"
							Content="..."
							Style="{StaticResource TransparentButton}" />
					</Grid>
				</GroupBox>

			</Grid>
		</GroupBox>

		<StackPanel Grid.Column="1">
			<GroupBox Style="{StaticResource PanelGroupBox}">
				<GroupBox.Header>
					<XivToolsWPF:Header
						Icon="Folder"
						Text="[Settings_FilesHeader" />
				</GroupBox.Header>

				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto" />
						<ColumnDefinition />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock
						Grid.Row="0"
						Grid.Column="0"
						Style="{StaticResource Label}"
						Text="[Settings_DefaultAuthor" />
					<TextBox
						Grid.Row="0"
						Grid.Column="1"
						Margin="6"
						Style="{StaticResource MaterialDesignTextBox}"
						Text="{Binding SettingsService.Settings.DefaultAuthor}" />

					<TextBlock
						Grid.Row="1"
						Grid.Column="0"
						Style="{StaticResource Label}"
						Text="[Settings_ShowFileExtensions" />
					<CheckBox
						Grid.Row="1"
						Grid.Column="1"
						Margin="6"
						IsChecked="{Binding SettingsService.Settings.ShowFileExtensions}" />

					<TextBlock
						Grid.Row="2"
						Grid.Column="0"
						Style="{StaticResource Label}"
						Text="[Settings_UseWindowsExplorer" />
					<CheckBox
						Grid.Row="2"
						Grid.Column="1"
						Margin="6"
						IsChecked="{Binding SettingsService.Settings.UseWindowsExplorer}" />

					<!--  Directory options  -->
					<GroupBox
						Grid.Row="3"
						Grid.ColumnSpan="2">
						<GroupBox.Header>
							<TextBlock Text="[Settings_Directories" />
						</GroupBox.Header>

						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<Grid.RowDefinitions>
								<RowDefinition />
								<RowDefinition />
								<RowDefinition />
								<RowDefinition />
							</Grid.RowDefinitions>

							<TextBlock
								Grid.Row="0"
								Grid.Column="0"
								Style="{StaticResource Label}"
								Text="[Settings_Dir_Characters" />
							<TextBox
								Grid.Row="0"
								Grid.Column="1"
								Margin="3,0,0,0"
								IsEnabled="False"
								Style="{StaticResource MaterialDesignTextBox}"
								Text="{Binding SettingsService.Settings.DefaultCharacterDirectory}" />
							<Button
								Grid.Row="0"
								Grid.Column="2"
								Margin="6,3,0,0"
								Click="OnBrowseCharacter"
								Content="..."
								Style="{StaticResource TransparentButton}" />

							<TextBlock
								Grid.Row="1"
								Grid.Column="0"
								Style="{StaticResource Label}"
								Text="[Settings_Dir_Poses" />
							<TextBox
								Grid.Row="1"
								Grid.Column="1"
								Margin="3,0,0,0"
								IsEnabled="False"
								Style="{StaticResource MaterialDesignTextBox}"
								Text="{Binding SettingsService.Settings.DefaultPoseDirectory}" />
							<Button
								Grid.Row="1"
								Grid.Column="2"
								Margin="6,3,0,0"
								Click="OnBrowsePose"
								Content="..."
								Style="{StaticResource TransparentButton}" />

							<TextBlock
								Grid.Row="2"
								Grid.Column="0"
								Style="{StaticResource Label}"
								Text="[Settings_Dir_CameraShots" />
							<TextBox
								Grid.Row="2"
								Grid.Column="1"
								Margin="3,0,0,0"
								IsEnabled="False"
								Style="{StaticResource MaterialDesignTextBox}"
								Text="{Binding SettingsService.Settings.DefaultCameraShotDirectory}" />
							<Button
								Grid.Row="2"
								Grid.Column="2"
								Margin="6,3,0,0"
								Click="OnBrowseCamera"
								Content="..."
								Style="{StaticResource TransparentButton}" />

							<!--
							<ana:TextBlock Grid.Row="2" Grid.Column="0" Text="[Settings_Dir_Scenes" Style="{StaticResource Label}"/>
							<TextBox Grid.Row="2" Grid.Column="1" Margin="3, 0, 0, 0" Style="{StaticResource MaterialDesignTextBox}" Text="{Binding SettingsService.Settings.DefaultSceneDirectory}" IsEnabled="False"/>
							<Button Grid.Row="2" Grid.Column="2" Margin="6, 3, 0, 0" Style="{StaticResource TransparentButton}" Content="..." Click="OnBrowseScene"/>
							-->

						</Grid>
					</GroupBox>
				</Grid>
			</GroupBox>

			<GroupBox Style="{StaticResource PanelGroupBox}">
				<GroupBox.Header>
					<XivToolsWPF:Header
						Icon="Question"
						Text="[Settings_PenumbraHeader" />
				</GroupBox.Header>

				<Grid Grid.Column="2">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto" />
						<ColumnDefinition />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock
						Grid.Row="0"
						Grid.Column="0"
						Style="{StaticResource Label}"
						Text="[Settings_UseExternalRefresh" />
					<CheckBox
						Grid.Row="0"
						Grid.Column="1"
						Margin="6"
						IsChecked="{Binding SettingsService.Settings.UseExternalRefresh}" />

					<XivToolsWPF:InfoControl
						Grid.Row="1"
						Grid.ColumnSpan="2"
						Width="237"
						Text="[Settings_UseExternalRefreshWarning" />
				</Grid>
			</GroupBox>

		</StackPanel>

		<GroupBox
			Grid.Column="2"
			Style="{StaticResource PanelGroupBox}">
			<GroupBox.Header>
				<XivToolsWPF:Header
					Icon="Keyboard"
					Text="[Settings_KeysHeader" />
			</GroupBox.Header>

			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition
						Height="Auto"
						ana:Behaviours.Tooltip="Settings_EnableHotkeys_Tooltip" />
					<RowDefinition
						Height="Auto"
						ana:Behaviours.Tooltip="Settings_EnableGameHotkeys_Tooltip" />
					<RowDefinition
						Height="Auto"
						ana:Behaviours.Tooltip="Settings_EnableForwardKeys_Tooltip" />
					<RowDefinition />
				</Grid.RowDefinitions>

				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<TextBlock
					Grid.Row="0"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_EnableHotkeys" />
				<CheckBox
					Grid.Row="0"
					Grid.Column="1"
					Margin="6"
					IsChecked="{Binding SettingsService.Settings.EnableHotkeys}" />

				<TextBlock
					Grid.Row="1"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_EnableGameHotkeys" />
				<CheckBox
					Grid.Row="1"
					Grid.Column="1"
					Margin="6"
					IsChecked="{Binding SettingsService.Settings.EnableGameHotkeyHooks}" />

				<TextBlock
					Grid.Row="2"
					Grid.Column="0"
					Style="{StaticResource Label}"
					Text="[Settings_EnableForwardKeys" />
				<CheckBox
					Grid.Row="2"
					Grid.Column="1"
					Margin="6"
					IsChecked="{Binding SettingsService.Settings.ForwardKeys}" />

				<ListBox
					x:Name="HotkeyList"
					Grid.Row="3"
					Grid.ColumnSpan="2">

					<ListBox.ItemContainerStyle>
						<Style
							BasedOn="{StaticResource MaterialDesignListBoxItem}"
							TargetType="ListBoxItem">
							<Setter Property="HorizontalContentAlignment" Value="Stretch" />
						</Style>
					</ListBox.ItemContainerStyle>

					<ListBox.GroupStyle>
						<GroupStyle>
							<GroupStyle.ContainerStyle>
								<Style TargetType="{x:Type GroupItem}">
									<Setter Property="Template">
										<Setter.Value>
											<ControlTemplate>
												<GroupBox
													Margin="3,6,3,0"
													Header="{Binding Name}">
													<ItemsPresenter Margin="6,0,0,0" />
												</GroupBox>
											</ControlTemplate>
										</Setter.Value>
									</Setter>
								</Style>
							</GroupStyle.ContainerStyle>
						</GroupStyle>
					</ListBox.GroupStyle>

					<ListBox.ItemTemplate>
						<DataTemplate>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition />
									<ColumnDefinition Width="75" />
								</Grid.ColumnDefinitions>

								<TextBlock
									Grid.Column="0"
									Text="{Binding Name}" />

								<StackPanel
									Grid.Column="1"
									Orientation="Horizontal">
									<StackPanel
										Orientation="Horizontal"
										Visibility="{Binding ModifierName, Converter={StaticResource NotNullToVisibilityConverter}}">
										<TextBlock Text="{Binding ModifierName}" />
										<TextBlock Text=", " />
									</StackPanel>
									<TextBlock Text="{Binding KeyName}" />
								</StackPanel>

							</Grid>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</Grid>
		</GroupBox>

	</Grid>
</local:PanelBase>
