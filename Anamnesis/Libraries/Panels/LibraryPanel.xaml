<panels:PanelBase
	x:Class="Anamnesis.Libraries.Panels.LibraryPanel"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:libraryItems="clr-namespace:Anamnesis.Libraries.Items"
	xmlns:local="clr-namespace:Anamnesis.Libraries.Panels"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:panels="clr-namespace:Anamnesis.Panels"
	Width="800"
	Height="450"
	CanResize="True"
	Icon="Book"
	mc:Ignorable="d">
	<Grid x:Name="ContentArea">
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="Auto"
				MinWidth="128" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<TreeView
			Grid.Column="0"
			ItemsSource="{Binding Library.Packs}"
			SelectedItemChanged="OnGroupSelected">

			<TreeView.Resources>
				<HierarchicalDataTemplate
					DataType="{x:Type libraryItems:Pack}"
					ItemsSource="{Binding Groups}">
					<TreeViewItem
						Background="LightGray"
						Header="{Binding Name}" />
				</HierarchicalDataTemplate>

				<HierarchicalDataTemplate
					DataType="{x:Type libraryItems:Group}"
					ItemsSource="{Binding Groups}">
					<TreeViewItem Header="{Binding Name}" />
				</HierarchicalDataTemplate>

				<DataTemplate DataType="{x:Type libraryItems:ItemBase}">
					<TreeViewItem Header="{Binding Name}" />
				</DataTemplate>

			</TreeView.Resources>

		</TreeView>

		<ListBox
			Grid.Column="1"
			ItemsSource="{Binding SelectedGroup.Items}" />

	</Grid>
</panels:PanelBase>
